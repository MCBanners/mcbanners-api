/**
 * A function that measures the rendered pixel width of a string.
 * Obtained from a canvas context after setting the desired font spec.
 */
export type TextMeasurer = (text: string) => number;
//# sourceMappingURL=measure.d.ts.map

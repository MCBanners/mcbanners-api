{
  "name": "@mcbanners/compat-runner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "compare": "bun run src/index.ts",
    "compare:help": "bun run src/index.ts --help",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  }
}
